# Slack user token used by scripts/dump-user-messages.js.
# Must be a user token (xoxp-...) — bot tokens don't have search.messages access.
# Required scopes: search:read, users:read, channels:history, groups:history, channels:read, groups:read.
SLACK_USER_TOKEN=xoxp-replace-me
